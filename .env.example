# Copy to .env.local to enable. Restart the dev server after changing.

# Default the map's live x/y % cursor-coordinate overlay ON ("true"|"false").
# Use it to read off exact percentage positions when adding markers to
# src/lib/maps/kowakujo.ts. The sidebar "Dev: show cursor coords" toggle
# still works on top of this.
NEXT_PUBLIC_SHOW_COORDS=false
